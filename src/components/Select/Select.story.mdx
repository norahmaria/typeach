import { Meta, Story } from '@storybook/addon-docs'

import Select from './TypeachSelect.vue'

<Meta title="Select" component={Select} />

export const Template = args => ({
  components: { Select },
  setup() {
    return { args }
  },
  template: '<Select v-bind="args" />',
})

<Story
  name="Select"
  args={{
    title: 'Hello World',
    id: '1',
  }}
>
  {Template.bind({})}
</Story>
